extends layout

block content
      // MY FUNCTIONS
      link(rel='stylesheet' href='./stylesheets/stylesheet.css')
      script(src='./javascripts/getTemperatures.js')
      script(defer src='./javascripts/renderingMap.js')
      script.
          var cities = !{cities};
          console.log("received data from DB:", cities)
      .container-fluid.bg-primary
        nav.navbar.navbar-expand-sm
          a.navbar-brand(href='/') Exercise 5
          button.navbar-toggler(type='button' data-bs-toggle='collapse' data-bs-target='#navbarItems' aria-controls='navbarItems' aria-expanded='false' aria-label='Toggle navigation')
            span.navbar-toggler-icon
          #navbarItems.collapse.navbar-collapse
            ul.navbar-nav.me-auto.mb-2.mb-lg-0
              li.nav-item
                a.nav-link(aria-current='page' href='/')  Home
              li.nav-item
                a.nav-link.active(href='/map')
                  | Map
                  span.sr-only  (current)
              li.nav-item
                a.nav-link(href='/chart') Chart
              li.nav-item
                a.nav-link(href='/impressum') Impressum 
      .container-fluid
        #map
         //div(id="map")

